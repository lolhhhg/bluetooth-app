# Bluetooth Manager Dependencies
# Core Bluetooth libraries
bleak>=0.21.0
bleak-winrt>=1.2.0; sys_platform == 'win32'

# System tray icon
pystray>=0.19.5
Pillow>=10.0.0

# GUI (built-in with Python)
# tkinter comes with Python on Windows

# Build tools for creating EXE
pyinstaller>=6.0.0

# Optional utilities
asyncio>=3.4.3
